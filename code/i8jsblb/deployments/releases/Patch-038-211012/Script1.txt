CREATE TABLE IBFT_STATUS
   (	"STAN" VARCHAR2(30 BYTE) NOT NULL ENABLE, 
	"REQ_TIME" VARCHAR2(40 BYTE) NOT NULL ENABLE, 
	"CREATED_ON" TIMESTAMP (6) DEFAULT systimestamp NOT NULL ENABLE, 
	 CONSTRAINT "IBFT_STATUS_PK" PRIMARY KEY ("STAN", "REQ_TIME"));
commit;